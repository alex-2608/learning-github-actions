name: Deployment Exercise 1
on: push
jobs:
  deploy:
    runs-on: ubuntu-lastest
    steps:
        - name: Get Code
          uses: actions/checkout@v3
        - name: install deps
          run: npm ci
        - name: run lint
          run: npm run lint
        - name: test code
          run: npm run test
        - name: build
          run: npm run build
        - name: deploye
          run: echo "deploying ..."
